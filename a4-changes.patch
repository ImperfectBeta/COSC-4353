diff --git a/QueueSmart.Api.Tests/DtoValidationTests.cs b/QueueSmart.Api.Tests/DtoValidationTests.cs
index a1c1b35..ba6857c 100644
--- a/QueueSmart.Api.Tests/DtoValidationTests.cs
+++ b/QueueSmart.Api.Tests/DtoValidationTests.cs
@@ -13,7 +13,7 @@ public class ModelAndDtoTests
     {
         var user = new User();
 
-        Assert.Equal(Guid.Empty, user.Id);
+        Assert.Equal(0, user.Id);
         Assert.Equal(string.Empty, user.Name);
         Assert.Equal(string.Empty, user.Email);
         Assert.Equal(string.Empty, user.PasswordHash);
@@ -23,7 +23,7 @@ public class ModelAndDtoTests
     [Fact]
     public void User_Properties_CanBeAssigned()
     {
-        var id = Guid.NewGuid();
+        var id = 42;
         var user = new User
         {
             Id = id,
diff --git a/QueueSmart.Api/Data/AppDbContext.cs b/QueueSmart.Api/Data/AppDbContext.cs
new file mode 100644
index 0000000..de9df0e
--- /dev/null
+++ b/QueueSmart.Api/Data/AppDbContext.cs
@@ -0,0 +1,26 @@
+using Microsoft.EntityFrameworkCore;
+using QueueSmart.Api.Models;
+
+namespace QueueSmart.Api.Data;
+
+public class AppDbContext : DbContext
+{
+    public AppDbContext(DbContextOptions<AppDbContext> options) : base(options) { }
+
+    public DbSet<UserCredential> UserCredentials => Set<UserCredential>();
+    public DbSet<UserProfile> UserProfiles => Set<UserProfile>();
+
+    protected override void OnModelCreating(ModelBuilder modelBuilder)
+    {
+        // Unique index on email
+        modelBuilder.Entity<UserCredential>()
+            .HasIndex(u => u.Email)
+            .IsUnique();
+
+        // 1-to-1: UserProfile shares PK with UserCredential
+        modelBuilder.Entity<UserCredential>()
+            .HasOne(c => c.Profile)
+            .WithOne(p => p.Credential)
+            .HasForeignKey<UserProfile>(p => p.Id);
+    }
+}
diff --git a/QueueSmart.Api/Models/UserCredential.cs b/QueueSmart.Api/Models/UserCredential.cs
new file mode 100644
index 0000000..ed9928e
--- /dev/null
+++ b/QueueSmart.Api/Models/UserCredential.cs
@@ -0,0 +1,20 @@
+using System.ComponentModel.DataAnnotations;
+using System.ComponentModel.DataAnnotations.Schema;
+
+namespace QueueSmart.Api.Models;
+
+[Table("UserCredentials")]
+public class UserCredential
+{
+    public int Id { get; set; }
+
+    [Required, MaxLength(200)]
+    public string Email { get; set; } = string.Empty;
+
+    [Required]
+    public string PasswordHash { get; set; } = string.Empty;
+
+    public UserRole Role { get; set; } = UserRole.User;
+
+    public UserProfile? Profile { get; set; }
+}
diff --git a/QueueSmart.Api/Models/UserProfile.cs b/QueueSmart.Api/Models/UserProfile.cs
new file mode 100644
index 0000000..f4caad2
--- /dev/null
+++ b/QueueSmart.Api/Models/UserProfile.cs
@@ -0,0 +1,22 @@
+using System.ComponentModel.DataAnnotations;
+using System.ComponentModel.DataAnnotations.Schema;
+
+namespace QueueSmart.Api.Models;
+
+[Table("UserProfiles")]
+public class UserProfile
+{
+    // Shared PK with UserCredential (1-to-1)
+    public int Id { get; set; }
+
+    [Required, MaxLength(100)]
+    public string FullName { get; set; } = string.Empty;
+
+    [MaxLength(200)]
+    public string? ContactInfo { get; set; }
+
+    [MaxLength(500)]
+    public string? Preferences { get; set; }
+
+    public UserCredential Credential { get; set; } = null!;
+}
diff --git a/QueueSmart.Api/Program.cs b/QueueSmart.Api/Program.cs
index 8f4dc4a..7f9dafd 100644
--- a/QueueSmart.Api/Program.cs
+++ b/QueueSmart.Api/Program.cs
@@ -1,4 +1,6 @@
 using System.Text.Json.Serialization;
+using Microsoft.EntityFrameworkCore;
+using QueueSmart.Api.Data;
 using QueueSmart.Api.Services;
 
 // builder is used to build the application
@@ -15,12 +17,16 @@ builder.Services.AddControllers()
 
 builder.Services.AddOpenApi();
 
+// PostgreSQL database
+builder.Services.AddDbContext<AppDbContext>(options =>
+    options.UseNpgsql(builder.Configuration.GetConnectionString("DefaultConnection")));
+
 // in-memory stores as singletons (a singleton is a class that can only have one instance)
 builder.Services.AddSingleton<IServiceStore, ServiceStore>();
 builder.Services.AddSingleton<IHistoryStore, HistoryStore>();
 builder.Services.AddSingleton<INotificationService, NotificationService>();
-builder.Services.AddSingleton<IUserStore, InMemoryUserStore>();
-builder.Services.AddSingleton<IAuthService, AuthService>();
+builder.Services.AddScoped<IUserStore, DbUserStore>();
+builder.Services.AddScoped<IAuthService, AuthService>();
 builder.Services.AddSingleton<IQueueService, QueueService>();
 
 // CORS policy
@@ -37,6 +43,13 @@ builder.Services.AddCors(options =>
 // app is the application
 var app = builder.Build();
 
+// ensure database is created
+using (var scope = app.Services.CreateScope())
+{
+    var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
+    db.Database.EnsureCreated();
+}
+
 // if the application is in development mode, map the OpenApi
 if (app.Environment.IsDevelopment())
 {
diff --git a/QueueSmart.Api/QueueSmart.Api.csproj b/QueueSmart.Api/QueueSmart.Api.csproj
index f1337a9..57d28b4 100644
--- a/QueueSmart.Api/QueueSmart.Api.csproj
+++ b/QueueSmart.Api/QueueSmart.Api.csproj
@@ -8,11 +8,7 @@
 
   <ItemGroup>
     <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.5" />
-    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.5">
-      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
-      <PrivateAssets>all</PrivateAssets>
-    </PackageReference>
-    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.5" />
+    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.1" />
   </ItemGroup>
 
 </Project>
diff --git a/QueueSmart.Api/Services/UserStore.cs b/QueueSmart.Api/Services/UserStore.cs
index a00f535..95240bb 100644
--- a/QueueSmart.Api/Services/UserStore.cs
+++ b/QueueSmart.Api/Services/UserStore.cs
@@ -1,3 +1,5 @@
+using Microsoft.EntityFrameworkCore;
+using QueueSmart.Api.Data;
 using QueueSmart.Api.Models;
 
 namespace QueueSmart.Api.Services;
@@ -11,6 +13,70 @@ public interface IUserStore
     bool EmailExists(string email);
 }
 
+public class DbUserStore : IUserStore
+{
+    private readonly AppDbContext _db;
+
+    public DbUserStore(AppDbContext db)
+    {
+        _db = db;
+    }
+
+    public User? GetByEmail(string email) =>
+        _db.UserCredentials
+           .Include(c => c.Profile)
+           .FirstOrDefault(c => c.Email == email.ToLowerInvariant())
+           .ToUser();
+
+    public User? GetById(int id) =>
+        _db.UserCredentials
+           .Include(c => c.Profile)
+           .FirstOrDefault(c => c.Id == id)
+           .ToUser();
+
+    public IEnumerable<User> GetAll() =>
+        _db.UserCredentials
+           .Include(c => c.Profile)
+           .AsEnumerable()
+           .Select(c => c.ToUser()!);
+
+    public User Add(User user)
+    {
+        var credential = new UserCredential
+        {
+            Email = user.Email,
+            PasswordHash = user.PasswordHash,
+            Role = user.Role,
+            Profile = new UserProfile { FullName = user.Name }
+        };
+
+        _db.UserCredentials.Add(credential);
+        _db.SaveChanges();
+
+        user.Id = credential.Id;
+        return user;
+    }
+
+    public bool EmailExists(string email) =>
+        _db.UserCredentials.Any(c => c.Email == email.ToLowerInvariant());
+}
+
+internal static class UserCredentialExtensions
+{
+    public static User? ToUser(this UserCredential? credential)
+    {
+        if (credential is null) return null;
+        return new User
+        {
+            Id = credential.Id,
+            Name = credential.Profile?.FullName ?? string.Empty,
+            Email = credential.Email,
+            PasswordHash = credential.PasswordHash,
+            Role = credential.Role
+        };
+    }
+}
+
 public class InMemoryUserStore : IUserStore
 {
     private readonly List<User> _users = new();
diff --git a/QueueSmart.Api/appsettings.json b/QueueSmart.Api/appsettings.json
index 10f68b8..d5343f5 100644
--- a/QueueSmart.Api/appsettings.json
+++ b/QueueSmart.Api/appsettings.json
@@ -1,4 +1,7 @@
 {
+  "ConnectionStrings": {
+    "DefaultConnection": "Host=localhost;Database=queuesmart;Username=postgres;Password=postgres"
+  },
   "Logging": {
     "LogLevel": {
       "Default": "Information",
diff --git a/QueueSmart.Tests/AuthServiceTests.cs b/QueueSmart.Tests/AuthServiceTests.cs
new file mode 100644
index 0000000..b8a7ef9
--- /dev/null
+++ b/QueueSmart.Tests/AuthServiceTests.cs
@@ -0,0 +1,152 @@
+using QueueSmart.Api.DTOs;
+using QueueSmart.Api.Services;
+
+namespace QueueSmart.Tests;
+
+public class AuthServiceTests
+{
+    private static AuthService CreateService() =>
+        new AuthService(new InMemoryUserStore());
+
+    // --- Register ---
+
+    [Fact]
+    public void Register_WithValidData_ReturnsAuthResponse()
+    {
+        var svc = CreateService();
+
+        var (response, error) = svc.Register(new RegisterRequest
+        {
+            Name = "Alice Smith",
+            Email = "alice@example.com",
+            Password = "Password1"
+        });
+
+        Assert.Null(error);
+        Assert.NotNull(response);
+        Assert.Equal("alice@example.com", response.Email);
+        Assert.Equal("Alice Smith", response.Name);
+        Assert.False(string.IsNullOrEmpty(response.Token));
+    }
+
+    [Fact]
+    public void Register_WithDuplicateEmail_ReturnsError()
+    {
+        var svc = CreateService();
+        svc.Register(new RegisterRequest { Name = "Alice", Email = "alice@example.com", Password = "Password1" });
+
+        var (response, error) = svc.Register(new RegisterRequest
+        {
+            Name = "Alice 2",
+            Email = "alice@example.com",
+            Password = "Password1"
+        });
+
+        Assert.Null(response);
+        Assert.NotNull(error);
+        Assert.Contains("already exists", error);
+    }
+
+    [Fact]
+    public void Register_WithMissingName_ReturnsError()
+    {
+        var svc = CreateService();
+
+        var (response, error) = svc.Register(new RegisterRequest
+        {
+            Name = "",
+            Email = "alice@example.com",
+            Password = "Password1"
+        });
+
+        Assert.Null(response);
+        Assert.NotNull(error);
+        Assert.Contains("Name is required", error);
+    }
+
+    [Fact]
+    public void Register_WithShortPassword_ReturnsError()
+    {
+        var svc = CreateService();
+
+        var (response, error) = svc.Register(new RegisterRequest
+        {
+            Name = "Alice",
+            Email = "alice@example.com",
+            Password = "short"
+        });
+
+        Assert.Null(response);
+        Assert.NotNull(error);
+        Assert.Contains("8 characters", error);
+    }
+
+    [Fact]
+    public void Register_WithInvalidEmail_ReturnsError()
+    {
+        var svc = CreateService();
+
+        var (response, error) = svc.Register(new RegisterRequest
+        {
+            Name = "Alice",
+            Email = "not-an-email",
+            Password = "Password1"
+        });
+
+        Assert.Null(response);
+        Assert.NotNull(error);
+        Assert.Contains("invalid", error, StringComparison.OrdinalIgnoreCase);
+    }
+
+    // --- Login ---
+
+    [Fact]
+    public void Login_WithValidCredentials_ReturnsAuthResponse()
+    {
+        var svc = CreateService();
+        svc.Register(new RegisterRequest { Name = "Bob", Email = "bob@example.com", Password = "Password1" });
+
+        var (response, error) = svc.Login(new LoginRequest
+        {
+            Email = "bob@example.com",
+            Password = "Password1"
+        });
+
+        Assert.Null(error);
+        Assert.NotNull(response);
+        Assert.Equal("bob@example.com", response.Email);
+    }
+
+    [Fact]
+    public void Login_WithWrongPassword_ReturnsError()
+    {
+        var svc = CreateService();
+        svc.Register(new RegisterRequest { Name = "Bob", Email = "bob@example.com", Password = "Password1" });
+
+        var (response, error) = svc.Login(new LoginRequest
+        {
+            Email = "bob@example.com",
+            Password = "WrongPassword"
+        });
+
+        Assert.Null(response);
+        Assert.NotNull(error);
+        Assert.Contains("Invalid", error);
+    }
+
+    [Fact]
+    public void Login_WithUnknownEmail_ReturnsError()
+    {
+        var svc = CreateService();
+
+        var (response, error) = svc.Login(new LoginRequest
+        {
+            Email = "nobody@example.com",
+            Password = "Password1"
+        });
+
+        Assert.Null(response);
+        Assert.NotNull(error);
+        Assert.Contains("Invalid", error);
+    }
+}
diff --git a/QueueSmart/src/routes/(app)/app/profile/+page.svelte b/QueueSmart/src/routes/(app)/app/profile/+page.svelte
index 675de73..b27b79a 100644
--- a/QueueSmart/src/routes/(app)/app/profile/+page.svelte
+++ b/QueueSmart/src/routes/(app)/app/profile/+page.svelte
@@ -1,4 +1,64 @@
-<div class="p-4 flex flex-col gap-4">
-	<a class="text-primary" href="/app">⇐ Back to app</a>
-	Profile page
+<script lang="ts">
+	import { get } from "svelte/store";
+	import { UserIcon, MailIcon, ShieldIcon } from "@lucide/svelte";
+
+	import { authSession } from "$lib/stores/auth";
+
+	const session = get(authSession);
+
+	const roleLabel: Record<string, string> = {
+		user: "User",
+		serviceAdmin: "Service Admin",
+		systemAdmin: "System Admin",
+	};
+
+	function formatRole(role: string): string {
+		return roleLabel[role] ?? role;
+	}
+</script>
+
+<div class="flex flex-col gap-6 max-w-lg">
+	<div>
+		<h2 class="text-2xl font-bold text-foreground">Profile</h2>
+		<p class="text-sm text-foreground/60 mt-1">Your account details</p>
+	</div>
+
+	{#if session}
+		<div class="rounded-xl border border-primary/20 bg-muted divide-y divide-primary/10">
+			<!-- Name -->
+			<div class="flex items-center gap-4 px-5 py-4">
+				<div class="flex-shrink-0 text-primary">
+					<UserIcon size={18} />
+				</div>
+				<div>
+					<p class="text-xs uppercase tracking-wider text-foreground/50 mb-0.5">Full Name</p>
+					<p class="text-sm font-medium text-foreground">{session.name}</p>
+				</div>
+			</div>
+
+			<!-- Email -->
+			<div class="flex items-center gap-4 px-5 py-4">
+				<div class="flex-shrink-0 text-primary">
+					<MailIcon size={18} />
+				</div>
+				<div>
+					<p class="text-xs uppercase tracking-wider text-foreground/50 mb-0.5">Email</p>
+					<p class="text-sm font-medium text-foreground">{session.email}</p>
+				</div>
+			</div>
+
+			<!-- Role -->
+			<div class="flex items-center gap-4 px-5 py-4">
+				<div class="flex-shrink-0 text-primary">
+					<ShieldIcon size={18} />
+				</div>
+				<div>
+					<p class="text-xs uppercase tracking-wider text-foreground/50 mb-0.5">Role</p>
+					<p class="text-sm font-medium text-foreground">{formatRole(session.role)}</p>
+				</div>
+			</div>
+		</div>
+	{:else}
+		<p class="text-sm text-foreground/60">No session found. Please log in again.</p>
+	{/if}
 </div>
